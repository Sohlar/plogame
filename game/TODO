Remove superfluous code (I need to strip out anything that isn't core gameplay and logging)
Create middleware plugins for websockets.
Generate Baseline models that will be included on build
Setup service distribution Dockerfile so that the whole package rolls from a container directly.
